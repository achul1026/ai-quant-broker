# ⚙️ OPERATIONS — ai-quant-broker 시스템 운영 가이드

> 자동매매 시스템을 안정적으로 무인 운용하기 위한 운영 정책·프로세스.
> 개발 순서는 [ROADMAP.md](ROADMAP.md), 자본·손실 리스크 관리는 [RISK.md](RISK.md) 참고.

자동매매 시스템은 "장중에 사람 없이 알아서 도는" 것이 목표다. 따라서 **장애가 곧 금전 손실**로 이어질 수 있어, 운영 안정성이 곧 시스템의 가치다.

| 영역 | 핵심 질문 | 도입 시점 |
| :--- | :--- | :--- |
| 1. 데이터 파이프라인 운영 | 데이터가 매일 빠짐없이 들어오는가? | Phase 1 |
| 2. 매매 시스템 운영 | 장 시작·중·마감을 올바르게 처리하는가? | Phase 4 |
| 3. 외부 API 운영 | API 장애·점검에 무너지지 않는가? | Phase 1~4 |
| 4. 모니터링 & 알림 | 문제를 즉시 인지하는가? | Phase 1~4 |
| 5. 인시던트 대응 | 장애 시 안전하게 멈추거나 복구하는가? | Phase 4 |
| 6. LLM 비용 운영 | 토큰 비용이 통제되는가? | Phase 2 |
| 7. 실행 환경 & 배포 | 어디서 어떻게 띄우는가? | Phase 4 |
| 8. 백업 & 로그 | 데이터·매매 기록이 보존되는가? | Phase 1~4 |

---

## 1. 데이터 파이프라인 운영 (Data Pipeline Ops)

**왜 중요한가**: 잘못되거나 누락된 데이터는 곧 잘못된 매매 신호로 이어진다.

- **ETL 정상성 점검**: 매일 장마감 후 ETL이 성공했는지 확인. 적재 건수·종목 수 검증.
- **데이터 누락·지연 감지**: 수집 실패 시 자동 재수집(backfill), 반복 실패 시 알림.
- **휴장일 처리**: 주말·공휴일·임시휴장일에는 ETL을 건너뛰도록 거래일 캘린더 관리.
- **데이터 품질 검증**: 이상치(0원, 비정상 급등락), 중복 적재, 분봉 결손 점검.

## 2. 매매 시스템 운영 (Trading System Ops)

- **장 시작 전(Pre-market)**: 자동 로그인, 토큰 발급, 전일 포지션·잔고 확인, 당일 감시 대상 종목 로드.
- **장중(Intraday)**: 실시간 시세 감시, 신호 발생 시 주문 전송, 체결 확인.
- **장 마감(Post-market)**: 당일 매매 결산, 포지션·손익 기록, 일일 리포트 생성.
- **증권사 점검 시간 대응**: 증권사 서버 정기 점검·주문 불가 시간대를 회피하도록 스케줄 설계.

## 3. 외부 API 운영 (External API Ops)

**왜 중요한가**: 증권사·뉴스·DART API는 통제 밖의 외부 의존성이다.

- **타임아웃·재시도**: 모든 외부 호출에 타임아웃과 지수 백오프 재시도 적용.
- **Rate Limit 관리**: 증권사 API 호출 한도를 초과하지 않도록 호출 빈도 제어.
- **OAuth 토큰 수명 관리**: 만료 전 자동 갱신, 갱신 실패 시 안전 대응.
- **API 장애 격리**: 뉴스/DART 장애가 매매 핵심 루프를 중단시키지 않도록 분리(감성 데이터 없으면 보수적 판단으로 폴백).

## 4. 모니터링 & 알림 (Monitoring & Alerting)

- **Slack 알림**: 주문 체결, 에러 발생, 일일 운용 리포트를 Slack으로 통지.
- **하트비트**: 시스템이 살아 있음을 주기적으로 보고. 하트비트 중단 시 즉시 경보.
- **핵심 지표 감시**: 일일 손익, 현재 포지션, API 에러율, ETL 성공 여부.

## 5. 인시던트 대응 (Incident Response)

**왜 중요한가**: 무인 운용 중 장애가 방치되면 의도치 않은 포지션·손실이 발생한다.

- **Retry Mechanism**: 일시적 오류는 재시도로 자가 복구. 셧다운 방지.
- **장애 등급**: (a) 자동 복구 가능 → 재시도, (b) 매매 중단 필요 → 신규 주문 정지·보유 유지, (c) 전면 정지 → 킬 스위치 발동([RISK.md](RISK.md)).
- **사후 리뷰**: 장애 발생 시 원인·대응을 기록하고 재발 방지책 반영.

## 6. LLM 비용 운영 (Cost Ops)

**왜 중요한가**: 안정성 서약 3 — API 비용 폭탄 방지.

- **입력 최소화**: 뉴스 텍스트 청킹·글자 수 제한으로 토큰 절감.
- **캐싱**: 동일 뉴스·동일 종목 재분석 시 결과 캐싱.
- **Structured Output**: JSON 규격 고정으로 불필요한 출력 토큰 절감.
- **비용 추적**: 일일 LLM 토큰 사용량·비용을 기록하고 임계치 초과 시 알림.

## 7. 실행 환경 & 배포 (Runtime & Deployment)

- **실행 위치 결정**: 로컬 PC 상시 구동 vs 클라우드/서버. 장중 무중단이 핵심 요건.
- **환경 분리**: 모의투자 환경과 실전 환경을 설정으로 명확히 분리. 실수로 실전 주문이 나가지 않도록 가드.
- **시간 동기화**: 서버 시각이 정확해야 장 시작/마감 스케줄이 어긋나지 않음.

## 8. 백업 & 로그 보존 (Backup & Logging)

- **DB 백업**: 시계열 주가 데이터, 매매 이력, 포지션 기록 정기 백업.
- **매매 로그**: 모든 주문·체결·판단 근거를 로그로 보존 (사후 분석·감사용).
- **복구 테스트**: 백업에서 실제로 복구되는지 주기적으로 확인.

---

## 운영 성숙도 진행

- **Phase 1**: 데이터 파이프라인 정상성 점검 + 기본 알림.
- **Phase 2**: LLM 비용 추적 시작.
- **Phase 3**: 매매 로그·백업 체계 정비.
- **Phase 4**: 매매 시스템 운영·외부 API 운영·인시던트 대응·모니터링 전면 가동 (무인 운용의 핵심 단계).
